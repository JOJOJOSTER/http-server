#ifndef HTTP_ENUMS
#define HTTP_ENUMS

namespace jojojoster::http {

// enum HTTP_METHODS
enum HTTP_METHODS_ENUM {
  GET,
  POST,
  PUT,
  DELETE,
  PATCH,
  HEAD,
  OPTIONAS,
  TRACE,
  CONNECT
};

enum HTTP_HEADERS_FIELD_ENUM { SERVER };

} // namespace jojojoster::http

#endif // !HTTP_ENUMS
